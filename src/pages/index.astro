---
// Native Astro redirect handled in astro.config.mjs (`redirects['/']`).
// This file kept as a fallback for adapters/hosts that don't run the redirect map.
return Astro.redirect('/home', 308);
---
